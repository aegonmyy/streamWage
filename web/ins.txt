Looking at the screenshot, two clear issues:

**
Two fixes needed on the worker dashboard mobile view:

**Fix 1 — Remove double navbar:**
The desktop navbar is still rendering on mobile alongside the new mobile navbar. Find the layout file where the original navbar renders — likely `app/dashboard/layout.tsx` or a shared layout component. Add `hidden md:flex` to the desktop navbar so it only shows on screens 768px and above. The mobile navbar should only show below 768px using `flex md:hidden`. Do not touch desktop layout.

**Fix 2 — Replace hamburger bottom sheet with floating nav pill:**

Remove the current hamburger icon and bottom sheet implementation entirely.

Replace with a **floating navigation pill** that is:
- `position: fixed`
- `bottom: 32px`
- `left: 50%` with `transform: translateX(-50%)` — centered horizontally
- `z-index: 50`
- Only visible on mobile (`flex md:hidden`)

**Collapsed state** (default):
- Circular button, `48px` diameter
- Shows a menu icon (`☰`) centered
- Background: dark (`#111`) with slight opacity
- Subtle box shadow to float above content
- `backdrop-filter: blur(8px)`

**Expanded state** (after tap):
- Pill expands horizontally with smooth animation (`transition: all 250ms ease`)
- Shows nav items inline: `Overview · Proposals · Help · Disconnect`
- Each item is tappable text, `14px`, white
- Separated by a dim `·` divider
- Pill background: dark with blur
- Tapping any item navigates and collapses pill back to icon
- Tapping anywhere outside the pill collapses it

**Animation:**
- Expand: scale from center outward, `250ms ease`
- Collapse: scale back to circle, `200ms ease`
- Use `useState` for open/closed, `useEffect` to add outside click listener

**Safe area:**
- Add `padding-bottom: env(safe-area-inset-bottom)` so pill never overlaps iPhone home indicator
- Adjust `bottom` value accordingly: `bottom: calc(32px + env(safe-area-inset-bottom))`

Only apply this on mobile. Desktop sidebar navigation remains completely unchanged.

